* Make bottom corners of Lush metacity theme use a gradient to get more 3D effect like original metacity theme
* Create new wm theme for BlueSphere that use bluesphere SVG icons for buttons
* Fix ugly kludge in Wasp buttons

